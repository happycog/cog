#!/bin/bash

# Variables
ARG_FILENAME=$1

if [ -f ".env" ]; then
    echo "⚠️  File already exist. Please try again"
    exit 0
fi
touch .env.example
echo -e "WEBSERVER_HOST=\nWEBSERVER_PORT=\nARG_DIR="> .env.example
echo '✨ .env.example was created. Rename the file to .env to use.'
