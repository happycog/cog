#!/bin/bash

# Variables
ARG_FILENAME=$1

if [ -f "Cogfile" ]; then
    echo "⚠️  File already exist. Please try again"
    exit 0
fi
touch Cogfile.example
echo -e "WEBSERVER_HOST=\nWEBSERVER_PORT=\nARG_DIR="> Cogfile.example
echo '✨ Cogfile.example was created. Rename the file to Cogfile to use.'
