#!/bin/bash

set -e

ARG_HOST=${WEBSERVER_HOST:-127.0.0.1}
ARG_PORT=${WEBSERVER_PORT:-8000}
ARG_DIR=${DOCUMENT_ROOT:-./}

if [ -e Cogfile ]; then
    source ./Cogfile
fi

pushd $ARG_DIR > /dev/null
ngrok http $ARG_HOST:$ARG_PORT
